# Prompt the user to enter their name and store it in the 'name' variable
name = input("Enter your name:\n")

# Calculate the length of the input name using the 'len' function
length = len(name)

# Print the result, including the name and its length
print("The length of " + name + " is " + str(length))
